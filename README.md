# CodeAlpha_MultiSendSmartContract
A Solidity smart contract that enables sending Ether to multiple Ethereum addresses in a single transaction. The contract accepts an array of recipient addresses, receives Ether through a payable function, distributes the Ether equally among all recipients, and verifies successful transfers using low-level calls.
